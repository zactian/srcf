(function ($) {

    $.fn.Append= function (options) {

        var settings = $.extend({}, options);
        var obj = this;

        var body = $("#college_pdfs");

        var ray = (function () {
            var that = this;
            that.main = {
                firstAction: function () {
                },
                secondAction: function () {
                },
                finalAction: function () {
                }
            }
            that.main.firstAction();
        })();
    };


}(jQuery));
